Olá aprovadores e administradores!

Notificamos que determinado contrato de nome <%= @contract.document.description %> está no seguinte estado: <%= final_state_contract(@contract.status)%>
Segue comentário(s) abaixo.


<% @comments.each do |c| %>
    Decisão: <%= c.decision ? "Aprovado" : "Reprovado" %>
    <%= c.user.first_name %> escreveu o seguinte:
    <%= c.observation %>
    Avaliação feita na data: <%= I18n.l(c.updated_at, format: :short) %>
<% end %>

Por favor, avaliar decisão e analisar o contrato citado em nosso sistema!